Поменять коллекцию удаленных элементов на 2:
- распределенные удаленые (только имена папок)
- сохраненные удаленые (только guid)
Добавить в коллекцию флаг isSave, изменить isChanged { set {
	if (val) {isSave = !val} 
	isChang = val;
	}}